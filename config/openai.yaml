# Aurora image generation provider configuration.
# API keys must never be committed. Set OPENAI_API_KEY in your shell.

provider: openai
model: gpt-image-1
quality: medium
number_of_images: 4
size: 1024x1024
background: transparent
